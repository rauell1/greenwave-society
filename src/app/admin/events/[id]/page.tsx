import { notFound } from "next/navigation"; import { requirePermission } from "@/lib/auth/guards"; import { PERMISSIONS } from "@/lib/auth/permissions"; import { isCmsFeatureEnabled } from "@/lib/cms/feature-flags"; import { getDb } from "@/lib/db"; import EventActions from "./EventActions";
export default async function EventPage({params}:{params:Promise<{id:string}>}) { await requirePermission(PERMISSIONS.EVENTS_READ); if(!(await isCmsFeatureEnabled("events")))notFound(); const {id}=await params; const event=await getDb().cmsEvent.findUnique({where:{id},include:{registrations:{orderBy:{createdAt:"desc"}},legacyBaraza:{orderBy:{createdAt:"desc"}}}}); if(!event)notFound(); return <section className="space-y-6"><div><a href="/admin/events" className="text-sm font-medium text-emerald-700">← Back to events</a><h1 className="mt-2 text-2xl font-bold">{event.title}</h1><p className="text-sm text-slate-500">{event.startsAt.toLocaleString("en-KE")} – {event.endsAt.toLocaleString("en-KE")} · {event.location||"Online"}</p></div><EventActions event={{id:event.id,status:event.status,capacity:event.capacity}}/><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Attendees ({event.registrations.length})</h2><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-2">Name</th><th className="p-2">Registration</th><th className="p-2">Attendance</th><th className="p-2"></th></tr></thead><tbody>{event.registrations.map(item=><tr key={item.id} className="border-t"><td className="p-2"><p className="font-medium">{item.fullName}</p><p className="text-xs text-slate-500">{item.email}</p></td><td className="p-2 capitalize">{item.status}</td><td className="p-2 capitalize">{item.attendanceStatus.replaceAll("_"," ")}</td><td className="p-2"><EventActions event={{id:event.id,status:event.status,capacity:event.capacity}} registrationId={item.id}/></td></tr>)}</tbody></table></div></div>{event.legacyBaraza.length>0&&<div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Linked legacy Baraza records ({event.legacyBaraza.length})</h2><p className="mt-2 text-sm text-slate-500">Preserved separately from CMS attendee records.</p></div>}</section>; }
