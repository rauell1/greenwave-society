"use client";
import { useRouter } from "next/navigation"; import { useState } from "react";
export default function EventActions({event,registrationId}:{event:{id:string;status:string;capacity:number|null};registrationId?:string}){const router=useRouter();const [message,setMessage]=useState("");const [form,setForm]=useState({fullName:"",email:"",phone:"",organization:""});async function send(url:string,body:unknown){const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();setMessage(response.ok?"Updated.":data.error);if(response.ok)router.refresh();}if(registrationId)return <div className="flex gap-2"><button onClick={()=>void send(`/api/admin/events/${event.id}/registrations/${registrationId}/attendance`,{attendanceStatus:"checked_in"})} className="text-xs font-medium text-emerald-700">Check in</button><button onClick={()=>void send(`/api/admin/events/${event.id}/registrations/${registrationId}/attendance`,{attendanceStatus:"no_show"})} className="text-xs text-slate-600">No-show</button></div>;return <div className="grid gap-4 lg:grid-cols-2"><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Lifecycle</h2><div className="mt-3 flex flex-wrap gap-2">{["published","cancelled","completed"].map(status=><button key={status} onClick={()=>void send(`/api/admin/events/${event.id}/status`,{status})} className="rounded-lg border px-3 py-2 text-sm capitalize">{status}</button>)}</div></div><div className="rounded-xl border bg-white p-5"><h2 className="font-semibold">Add attendee</h2><div className="mt-3 grid gap-2"><input placeholder="Full name" value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})} className="rounded border p-2"/><input type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="rounded border p-2"/><input placeholder="Phone" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} className="rounded border p-2"/><button onClick={()=>void send(`/api/admin/events/${event.id}/registrations`,form)} className="rounded bg-slate-900 p-2 text-sm text-white">Register attendee</button></div>{message&&<p className="mt-2 text-sm">{message}</p>}</div></div>;}
