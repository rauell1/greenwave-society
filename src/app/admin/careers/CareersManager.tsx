"use client";
import { useEffect,useState } from "react";
type Role={slug:string;title:string;isOpen:boolean};
export default function CareersManager(){const[roles,setRoles]=useState<Role[]>([]);const[key,setKey]=useState("");const[message,setMessage]=useState("");async function load(){const r=await fetch('/api/admin/careers/roles');const d=await r.json();setRoles(d.roles??[]);setMessage(r.ok?'':d.error)}useEffect(()=>{void load()},[]);async function toggle(role:Role){const r=await fetch('/api/admin/careers/roles',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({slug:role.slug,isOpen:!role.isOpen})});const d=await r.json();setMessage(r.ok?`${role.title} is now ${!role.isOpen?'open':'closed'}.`:d.error);if(r.ok)await load()}async function download(){const r=await fetch('/api/admin/careers/export',{headers:{'x-export-key':key}});if(!r.ok){const d=await r.json();setMessage(d.error);return}const blob=await r.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='greenwave-careers-applications.xls';a.click();URL.revokeObjectURL(url)}return <div className="space-y-6">{message&&<p role="status" className="rounded-lg bg-slate-100 p-3 text-sm">{message}</p>}<section className="rounded-xl border bg-white p-5"><h2 className="text-lg font-semibold">Role availability</h2><div className="mt-4 grid gap-3">{roles.map(role=><div key={role.slug} className="flex flex-wrap items-center justify-between gap-4 rounded-lg border p-4"><div><span className={`rounded-full px-2 py-1 text-xs font-bold ${role.isOpen?'bg-emerald-100 text-emerald-800':'bg-red-100 text-red-800'}`}>{role.isOpen?'Open':'Closed'}</span><h3 className="mt-2 font-semibold">{role.title}</h3></div><button onClick={()=>void toggle(role)} className="rounded-lg border px-4 py-2 text-sm font-medium">{role.isOpen?'Close role':'Open role'}</button></div>)}</div></section><section className="rounded-xl border bg-white p-5"><h2 className="text-lg font-semibold">Application workbook</h2><p className="mt-2 text-sm text-slate-500">Download one workbook with a separate sheet for each role.</p><input type="password" value={key} onChange={e=>setKey(e.target.value)} placeholder="Export password" className="mt-4 w-full max-w-md rounded-lg border px-3 py-2"/><div><button disabled={!key} onClick={()=>void download()} className="mt-3 rounded-lg bg-emerald-700 px-4 py-2 text-sm font-medium text-white disabled:opacity-50">Download Excel workbook</button></div></section></div>}
